experiment:
  name: baseline
  seed: 42

data:
  train_path: "data/processed/train.csv"
  target_column: "target"

model:
  type: "logistic_regression"
  params:
    max_iter: 200
    n_jobs: -1

validation:
  test_size: 0.3
  shuffle: true
